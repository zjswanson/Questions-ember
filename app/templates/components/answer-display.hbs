<div class="col-sm-3">
  <div class="answer-display">
    <h4>{{answer.answerTitle}}</h4>
    <h5>By: {{answer.author}}</h5>
    <p>
      {{answer.answerBody}}
    </p>
    <button {{action "deleteAnswer" answer question}} class="btn btn-sm">Delete</button>
  </div>
</div>
